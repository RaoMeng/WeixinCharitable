// component/numberView/numberView.js
Component({
     externalClasses: ['parentClass'],

     /**
      * 组件的属性列表
      */
     properties: {
          resArray: {
               type: Array,
               value: [],
          }
     },

     /**
      * 组件的初始数据
      */
     data: {

     },

     /**
      * 组件的方法列表
      */
     methods: {

     }
})